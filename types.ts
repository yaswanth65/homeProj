export interface Project {
  id: string;
  title: string;
  category: string;
  imageUrl: string;
}

export interface AccordionItem {
  id: string;
  number: string;
  title: string;
  content: string;
}

export interface ProcessStep {
  id: string;
  number: string;
  title: string;
  description: string;
  imageUrl: string;
}